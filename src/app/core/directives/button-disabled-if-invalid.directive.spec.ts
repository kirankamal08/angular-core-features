import { ButtonDisabledIfInvalidDirective } from './button-disabled-if-invalid.directive';

describe('ButtonDisabledIfInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonDisabledIfInvalidDirective();
    expect(directive).toBeTruthy();
  });
});
